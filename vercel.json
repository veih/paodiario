{
    "buildCommand": "node scripts/generateStaticData.mjs && npx expo export --platform web && cp -r public/data dist/",
    "outputDirectory": "dist",
    "installCommand": "npm install",
    "framework": null
}