{
    "buildCommand": "npx expo export --platform web && node scripts/copyDataToDist.js",
    "outputDirectory": "dist",
    "installCommand": "npm install",
    "framework": null,
    "rewrites": [
        {
            "source": "/data/(.*)",
            "destination": "/data/$1"
        }
    ]
}